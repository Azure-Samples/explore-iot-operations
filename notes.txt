
First Tool:
1. HTTP Input (configure what the data looks like)
2. GRPC/HTTP Output (output needs to go to stdout and also mqtt)
3. HTTP Callout w/ logging to stdout
(missing items) HTTP Callout anomaly detection (document sent by Ud), GRPC callout shift determination (takes a source timestamp and responds with the shift number 8 hour shifts with three starting at 12am)

SQL Tool:
4. SQL Input (Preconfigured with data, can configure with the data they want) (kubectl run sql-callout ... < sql.db)

wget https://github.com/azure-samples/explore-iot-operations/samples/callout/config.yml
kubectl run grpc-callout azbluefin.azurecr.io/callout:latest --stdin < config.yml

